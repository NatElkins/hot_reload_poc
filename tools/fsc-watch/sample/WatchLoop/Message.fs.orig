namespace WatchLoop

open System

module Target =
    let mutable counter = 0

    let GetMessage () =
        counter <- counter + 1
        sprintf "Message version hashwait 1 (invocation #%d)" counter
